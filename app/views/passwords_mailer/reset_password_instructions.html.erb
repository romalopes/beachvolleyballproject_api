<p>
  You can reset your password on
  <%= link_to "this password reset page",
    "#{ENV.fetch("FRONTEND_URL", "http://localhost:5174")}/reset-password?token=#{@user.password_reset_token}" %>.
  This link will expire in <%= distance_of_time_in_words(0, @user.password_reset_token_expires_in) %>.
</p>
<p>Hello <%= @user.email_address %>!</p>
<p>Someone requested a link to change your Beach Volleyball Project password. You can do this through the link below.</p>
<p><%= link_to "this password reset page",
    "#{ENV.fetch("FRONTEND_URL", "http://localhost:5174")}/reset-password?token=#{@user.password_reset_token}" %>.</p>
<p>This link will expire in <%= distance_of_time_in_words(0, @user.password_reset_token_expires_in) %>.</p>
<p>If you didn't request this, please ignore this email. Your password won't change until you access the link above and create a new one.</p>